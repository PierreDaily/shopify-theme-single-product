// Put your styles in this file.
// Shopify will compile SCSS/SASS on each deploy.
// Note: "@import" rules aren’t supported.

// GLOBAL
$font-size-global: 1.5rem;
$line-height-global: 2rem;
$gutter-global: 4rem;

//  FONT
$primary-font: Rubik;

// COLOR
$cart-button: #d8d8d8;
$cart-content: #9a9a9a;
$black: #000000;
$black-light: #555555;
$grey: #707070;
$grey-light: #979797;
$orange: #ff3700;
$white: #ffffff;

// BORDER
$black-border: 1px solid $black;
$grey-border: 1px solid $grey-light;
$white-border: 1px solid $white;

html {
  /* set base font-size to equiv "10px", which is adapted to rem unit */
  font-size: 62.5%;
  /* IE9-IE11 math fixing. See http://bit.ly/1g4X0bX */
  font-size: calc(1em * 0.625);
  height: 100%;
}

@media only screen and (min-width: 1921px) {
  html {
    font-size: 80%;
  }
}

.button {
  align-items: center;
  border: none;
  color: $white;
  display: flex;
  font-size: $font-size-global;

  height: 6rem;
  justify-content: space-between;
  line-height: 1.8rem;
  letter-spacing: 0;
  padding: 0 2rem 0 2rem;
  width: 100%;
}

.button--add-to-cart {
  background-color: $orange;
}

.button--checkout {
  background-color: $black;
  font-style: normal;
  font-weight: normal;
  padding: 0 0 0 2rem;
}

.cart-list {
  padding: 0;
  margin: 0 0 10rem 0;
}

.cart-list__elem {
  color: $white;
  display: flex;
  justify-content: space-between;
  font-style: normal;
  font-weight: 300;
  font-size: $font-size-global;
  line-height: 1.8rem;
  list-style-type: none;
  /* or 120% */

  text-align: left;
  border-bottom: $white-border;
  padding-bottom: 2rem;
  padding-top: 2rem;
  width: 100%;
}

.container {
  display: flex;
  flex-direction: row;
  padding: $gutter-global $gutter-global $gutter-global 0;
  font-family: $primary-font;
  height: 100vh;
  box-sizing: border-box;
  position: relative;
}

.container--cart {
  padding: 0;
}

.detail {
  display: flex;
  flex-direction: row;
  margin-left: $gutter-global;
  min-height: 40rem;
}

.detail--cart {
  margin-right: 4rem;
  margin-bottom: 4.3rem;
}

.detail__left {
  width: 50%;
  margin-right: $gutter-global;
  margin-bottom: 6rem;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
}

.detail__left--cart {
  margin-bottom: 0;
  margin-right: 1rem;
}

.detail__right {
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  width: 50%;
}

.detail__right--cart {
  margin-left: 1rem;
}

.description {
  margin-bottom: $gutter-global;
}

.description--cart {
  margin-bottom: 16rem;
}

.description__content {
  color: $black-light;
  font-size: $font-size-global;
  font-style: normal;
  font-weight: 400;
  line-height: $line-height-global;
  letter-spacing: 0px;
  text-align: left;
}

.description__content--cart-active {
  color: $white;
  display: block;
}

.description__headline {
  color: $black;
  font-size: $font-size-global;
  font-style: normal;
  font-weight: 400;
  line-height: $line-height-global;
  letter-spacing: 0px;
  text-align: left;
  margin-top: 0;
}

.label-color {
  color: $grey;
  display: block;
  font-size: 1.2rem;
  margin-bottom: 0.5rem;
}

.info-list {
  padding: 0;
  margin: 0;
}

.info-list__elem {
  color: $black-light;
  font-style: normal;
  font-weight: 300;
  font-size: $font-size-global;
  line-height: 1.8rem;
  list-style-type: none;
  /* or 120% */

  text-align: left;
  border-bottom: $grey-border;
  padding-bottom: 2rem;
  padding-top: 2rem;

  &:first-child {
    border-top: $grey-border;
  }
}

.main-title {
  font-size: 39rem;
  font-style: normal;
  font-weight: 300;
  line-height: 32rem;
  letter-spacing: -1.7rem;
  text-align: center;
  margin: 0 0 6rem 0;
}

.main-title--cart-active {
  color: $white;
  font-size: 33.5rem;
  font-weight: normal;
  letter-spacing: -1.5rem;
}

.product-display {
  width: 50%;
  object-fit: cover;
}

.product-display--cart {
  padding-bottom: 4rem;
  padding-top: 4rem;
}

.right-column {
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  width: 50%;
  overflow-x: hidden;
}

.right-column--cart-active {
  background-color: $orange;
}

.subitle {
  font-family: $primary-font;
  font-style: normal;
  font-weight: normal;
  font-size: 3rem;
  line-height: $gutter-global;
  max-width: 23rem;
  margin-bottom: 5rem;
  color: #232323;
}

.subitle--cart {
  color: $white;
  max-width: 24.5rem;
}

.my-cart {
  align-items: center;
  display: flex;
  justify-content: flex-end;
  height: 2rem;
  position: absolute;
  right: 4rem;
  text-decoration: none;
  top: 4rem;

  &:hover {
    .my-cart__content {
      visibility: visible;
    }
  }
}

.my-cart__logo {
  background-color: $cart-button;
  border-radius: 50%;
  display: block;
  height: 1rem;
  width: 1rem;
}

.my-cart__logo--active {
  background-color: $orange;
  color: $white;
  border-radius: 50%;
  display: block;
  line-height: $line-height-global;
  height: 2rem;
  text-align: center;
  width: 2rem;
}

.my-cart__content {
  color: $cart-content;
  font-size: $font-size-global;
  line-height: $line-height-global;
  letter-spacing: 0;
  margin-right: 2rem;
  text-align: left;
  visibility: hidden;
}

.select-group {
  position: relative;
  div {
    display: flex;
    align-items: center;
    justify-content: center;
    position: absolute;
    top: 0;
    right: 0;
    width: 6rem;
    height: 6rem;
    border-left: $black-border;
    pointer-events: none;
    img {
      display: inline-block;
      vertical-align: center;
      text-align: center;
    }
  }

  select {
    border: $black-border;
    appearance: none;
    height: 6rem;
    width: 100%;
    font-size: $font-size-global;
    line-height: 1.8rem;
    letter-spacing: 0px;
    padding-left: 2rem;
    text-align: left;
  }
}

@media only screen and (max-height: 860px) {
  html {
    font-size: 50%;
  }

  .main-title {
    font-size: 25rem;
  }
}
